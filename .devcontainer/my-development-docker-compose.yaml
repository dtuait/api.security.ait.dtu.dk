
# development command - the only differnce is command in xx-api-security-ait-dtu-dk-app-main is commented out
# docker compose -p dev-api-security-ait-dtu-dk -f ./my-development-docker-compose.yaml up -d
# production command
# docker compose -p prod-api-security-ait-dtu-dk -f ./my-production-docker-compose.yaml up -d

services:
  xx-api-security-ait-dtu-dk-app-main:
    image: dtuait/api-security-ait-dtu-dk-app-main:python-3.10-bullseye-django-4.2.11-27-03-2024-venv-myversion-1.0.16
    user: dockeruser

    # development command
    command: sleep infinity
    # production command
    # command: 'bash -c "cd /usr/src/project/app-main && source /usr/src/venvs/app-main/bin/activate && python3 manage.py runserver 0.0.0.0:8121 --settings app.production_settings"'


    restart: always
    tty: true
    volumes:
      - ..:/usr/src/project
      - ../shared-project-data:/mnt/shared-project-data
      # - ./entrypoint.sh:/entrypoint.sh # you dont need to mount it since it is already in the image
    environment:
      - HOST_UID=${DOCKERUSER_UID} # Replace with the host's UID - dockeruser I alwways use 65000
      - HOST_GID=${DOCKERUSER_GID} # Replace with the host's GID - dockeruser I alwways use 65000
    networks:
      - shared_network

##### only for production ####
  xx-api-security-ait-dtu-dk-httpd-service:
    image: dtuait/api-security-ait-dtu-dk-httpd-service:httpd-2.4-myversion-1.0.0
    restart: always
    volumes:
      - ../shared-project-data:/mnt/shared-project-data
      - ../httpd-service/data/virtualhosts:/usr/local/apache2/conf/virtualhosts
      - ../httpd-service/data/httpd.conf:/usr/local/apache2/conf/httpd.conf
    networks:
      - shared_network
##### only for production ####

  xx-api-security-ait-dtu-dk-mariadb-service:
    image: mariadb:11.3.2
    restart: always
    environment:
      PMA_HOST: ${MYSQL_HOST}
      MYSQL_ROOT_PASSWORD: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - beta-api-security-ait-dtu-dk-mariadb-service-volume:/var/lib/mysql
      - ../mariadb-service/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - shared_network

  xx-api-security-ait-dtu-dk-phpmyadmin-service:
      image: phpmyadmin/phpmyadmin:5.2.1
      restart: always
      ports:
        - "5010:80"
      environment:
        PMA_HOST: ${MYSQL_HOST} # beta-api-security-ait-dtu-dk-mariadb-service
        MYSQL_ROOT_PASSWORD: ${MYSQL_DATABASE}
        MYSQL_USER: ${MYSQL_USER}
        MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      volumes:
        - ../phpmyadmin-service/phpmyadmin-misc.ini:/usr/local/etc/php/conf.d/phpmyadmin-misc.ini
      networks:
        - shared_network

networks:
  shared_network:
    external: true

volumes:
  beta-api-security-ait-dtu-dk-mariadb-service-volume:
    external: true
