version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"  # The root directory of your project
    schedule:
      interval: "weekly"
    ignore:
      - dependency-name: "*"
        paths:
          - ".devcontainer/.docker-image-builder-archive/*"
